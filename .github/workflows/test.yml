# This workflow will verify the exercises in the repository.
#
# Requires scripts:
# - bin/verify-exercises

name: Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  verify_exercises:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Use nushell
        run: |
          curl -L -o nu.tar.gz https://github.com/nushell/nushell/releases/download/0.114.1/nu-0.114.1-x86_64-unknown-linux-gnu.tar.gz && \
          tar -xzf nu.tar.gz && \
          sudo mv nu-0.114.1-x86_64-unknown-linux-gnu/* /usr/bin/ && \
          rm nu.tar.gz && \
          rm -r nu-0.114.1-x86_64-unknown-linux-gnu


      - name: Verify all exercises
        run: bin/verify-exercises
